/* Importação */
const { error, errorHandling, refError } = require("./error");
const { asyncResp } = require("./asyncResp");

/* Exportação */
module.exports = {
  error,
  errorHandling,
  refError,
  asyncResp,
};
